# Theme Picker

A simple minimalist Next.js app that analyzes a target URL and surfaces theme signals:

- Fonts used (best-effort)
- Colors used (best-effort)
- Theme direction (light/dark/mixed)

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
